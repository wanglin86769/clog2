module.exports = {
  // Mail server
  host: "mail.ihep.ac.cn",
  port: "25",
  secure: false, // Use `true` for port 465, `false` for all other ports

  // Email user
  user: "email_address",
  name: "Clog Notification",
  pass: "email_password"
}
